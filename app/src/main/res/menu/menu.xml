<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:title="Principal">
        <menu>
            <item android:id="@+id/btnMenuPrincipal"
                android:icon="@drawable/ic_home"
                android:title="Menú principal"/>
            <item
                android:id="@+id/registrarCliente"
                android:icon="@drawable/ic_group"
                android:title="Cambiar contraseña">
            </item>
            <item
                android:id="@+id/acerca"
                android:icon="@drawable/ic_group"
                android:title="Acerca de grupo">
            </item>
            <item
                android:id="@+id/bnsalir"
                android:icon="@drawable/ic_logout"
                android:title="@string/btnSalir"></item>
        </menu>

    </item>
    <!--item android:title="Secundario">
        <menu>
            <item
                android:id="@+id/home1"
                android:icon="@drawable/ic_home"
                android:title="Home">
            </item>
            <item
                android:id="@+id/acerca1"
                android:icon="@drawable/ic_group"
                android:title="Acerca de grupo">
            </item>
        </menu>
    </item-->
</menu>